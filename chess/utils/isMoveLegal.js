// This function is passed the moves array from processMoves function. It will return only the legal moves considering the following characters: is there terrain on the way? are there pieces on the way? can they be captured? etc.

function isMoveLegal(movesArray, board) {}

module.exports = isMoveLegal;
